# 05 — Cloudflare hosting target + DNS cutover
Labels: `wayfinder:research` · Blocks: — · Blocked by: — · Assignee: —
Note: research ticket — resolvable in parallel via a /research subagent.

## Question
Cloudflare Workers static assets vs Pages for a static Astro build (2026 recommendation),
`wrangler` config shape, build command, and the DNS/domain cutover steps off Vercel
including zero-downtime ordering and where the apex currently points.
