# 06 — Old URL redirects / SEO
Labels: `wayfinder:grilling` · Blocks: — · Blocked by: 01 · Assignee: —

## Question
`/contratacion`, `/booking`, `/booking/gracias` are indexed and possibly deep-linked.
301 to `/` or to `/#<anchor>`? Where are redirects declared on Cloudflare?
Does `sitemap.xml.ts` become a static file?
